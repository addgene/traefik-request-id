name: Security Scan Repo

on:
  push:
    branches:
      - main
      - master
      - develop
      - tools
      - release/*
  schedule:
    - cron: "0 10 * * 1"

permissions:
  contents: read
  pull-requests: write
  security-events: write
  checks: write
  actions: read

jobs:
  security_scan:
    uses: ZeroGachis/.github/.github/workflows/security_scan_repo.yml@cdd00be3df78755fb8b4e6e4756402d6231478d0 # v4
    secrets: inherit
